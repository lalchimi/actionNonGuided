name: CI Simple

on: [push]

jobs:
  build:
    runs-on: unbuntu-latest

    steps:
    - name: Checkout du code
      uses: actions/checkout@v4

    - name: "log a text"
      run: echo "Hello, world !"
